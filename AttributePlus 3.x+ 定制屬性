定制價格表:
基礎屬性 - 8 CNY / 單條
複雜屬性 - 15 CNY / 單條

如何定義"基礎屬性及複雜屬性"？
首先，顧名思義，基礎屬性並不包含複雜邏輯，例如"攻擊力、防御力"。只用於單一的修正傷害作用，而無特殊效果。
而複雜屬性，例如熟知的遊戲 LOL 中的天賦 "征服者"。其效果為，每次造成傷害是進行疊層，每層都給予攻擊者一定增益，並且滿層後有特殊效果。
其中就包括了疊層、增益、滿層效果等邏輯。因此被定義為復雜屬性。

如何定義"單條"？
首先在 AttributePlus 3.x 版本中，創建任何自定義屬性(腳本)都需要一個主屬性，該主屬性被視為"一條"。
其次，若需創建配合其效果的OTHER類子屬性，則需要用到方法 registerOtherAttribute()。其每條也被是作為"一條"。
在以下教學案例中，由於沒有複雜內容(特別邏輯等)則視作為2條基礎屬性，即 16 CNY。

var priority = 5
var combatPower = 1.0
var attributeName = "首領傷害"
var attributeType = "ATTACK"
var placeholder = "boss_damage"

function onLoad() {
    Utils.registerOtherAttribute("首領標籤", 10.0, "boss_tag");
	return Attr;
}

function runAttack(attacker, entity, handle){
	var addition = Attr.getRandomValue(attacker, "首領傷害", handle) / 100; 
	var tag = Attr.getRandomValue(entity, "首領標籤", handle);
	var damage = Attr.getDamage(attacker);
  var finalDmg = 0;
    
	if (addition){
		if (tag > 0){
			finalDmg = damage * addition;
			attacker.sendMessage("§7[§6§l!§7] §7攻擊目標為 §c首领 §7属性 §e首領傷害 §7生效 §f>§7>§8> §b" + addition * 100 + "§3%");
		}
	}
	
    Attr.addDamage(attacker, finalDmg, handle);
    return false;
}
